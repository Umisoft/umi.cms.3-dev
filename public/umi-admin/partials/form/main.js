define([
    'App', './element/main', './formBase/main', './formControl/main'
], function(UMI, element, formBase, formControl) {
        'use strict';

        element();
        formBase();
        formControl();
    });