{{ include('form/element/input.twig', {'element' : element}) }}

<div class="form-group {{ not element.valid ? 'has-error' }}">
    <label>{{ translate('PasswordConfirmation') }}</label>
    <input class="form-control" {{element.attributes | raw }} />
</div>