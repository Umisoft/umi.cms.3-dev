{{ include('breadcrumbs.twig') }}

<h1>{{ page.getHeader() }}</h1>
{{ page.contents }}

<h2>{{ translate('All news') }}</h2>
{{ widget('news.item.list', {
    'limit' : 3,
    'pagination' : {
        'pageParam' : 'p',
        'type' : 'sliding',
        'pagesCount' : 5
    }
}) }}