{{#if model}}
    <div class="umi-overlay"></div>
    <div {{bind-attr class=":umi-dialog model.type"}}>
        {{#if model.close}}
            <a href="" {{action 'close' model target="view"}} class="close">&times;</a>
        {{/if}}
        {{yield}}
    </div>
{{/if}}