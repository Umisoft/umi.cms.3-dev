<?php
/**
 * @var umicms\project\module\news\api\object\NewsRubric $rubric
 * @var umi\orm\selector\ISelector $rubrics
 */
?>

<?php if ($rubrics->getTotal()) :?>

    <ul>
        <?php foreach ($rubrics as $rubric) : ?>
            <li>
                <?= $this->widget('news.rubric.view', ['rubric' => $rubric, 'template' => 'shortView']); ?>
                <?= $this->widget('news.rubric.rss', ['rubric' => $rubric, 'template' => 'rssLink']); ?>
            </li>
        <?php endforeach;?>
    </ul>
<?php endif; ?>