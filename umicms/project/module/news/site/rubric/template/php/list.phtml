<?php
/**
 * @var umicms\project\module\news\object\NewsRubric $rubric
 * @var umi\orm\selector\ISelector $rubrics
 */
?>

<?php if ($rubrics->getTotal()) :?>

    <ul>
        <?php foreach ($rubrics as $rubric) : ?>
            <li>
                <?= $this->widget('view', ['guid' => $rubric->guid, 'template' => 'shortView']); ?>
            </li>
        <?php endforeach;?>
    </ul>
<?php endif; ?>