<?php
/**
 * @var array $pagination
 */
?>

<? if (isset($pagination['pagesCount'])) :?>

    <ul>
        <? foreach ($pagination['pagesRange'] as $page => $link) : ?>

            <? if ($page != $pagination['currentPage']) :?>
                <li><a href="<?= $link ?>"><?= $page ?></a></li>
            <? else : ?>
                <li><?= $page ?></li>
            <? endif; ?>

        <? endforeach;?>
    </ul>
<? endif; ?>