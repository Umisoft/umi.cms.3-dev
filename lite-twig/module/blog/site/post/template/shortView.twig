<h3>
    <a href="{{ blogPost.getPageUrl() }}">
        {{ blogPost.h1 }}
    </a>
</h3>
<time datetime="{{ blogPost.publishTime.format('c') }}">
    {{ blogPost.publishTime.format('d.m.Y | H:i') }}
    Автор: {{ blogPost.author.displayName }}
</time>
{{ blogPost.announcement }}