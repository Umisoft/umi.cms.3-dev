<a href="{{ rubric.getPageUrl() }}">
    {{ rubric.displayName }}
</a> {{ widget('news.rubric.rss', {'rubric' : rubric, 'template' : 'rssLink'}) }}
{{ widget('news.rubric.list', {'parentRubric' : rubric, 'template' : 'listShort'}) }}