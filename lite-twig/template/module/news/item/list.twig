{% if (list.getTotal()) %}
    {% for newsItem in list %}
        {{ widget('news.item.view', {'newsItem' : newsItem, 'template' : 'shortView'}) }}
    {% endfor %}

    {{ include('pagination.twig', {'paginator' : paginator}) }}
{% endif %}