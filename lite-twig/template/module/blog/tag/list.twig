{% if (list.getTotal()) %}
    <ol class="list-unstyled">
        {% for blogTag in list %}
            <li>
                {{ widget('blog.tag.view', {'blogTag' : blogTag, 'template' : 'shortView'}) }}
            </li>
        {% endfor %}
    </ol>
{%  endif %}