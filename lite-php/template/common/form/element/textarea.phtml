<?php
/**
 * @var \umi\form\element\Textarea $textarea
 */
$attributes = array_merge(
    $textarea->getAttributes(),
    ['name' => $textarea->getElementName()]
);

echo '<div class="form-group"><label>' . $textarea->getName() . '</label><textarea class="form-control" rows="4" ' . buildAttributes($attributes) . ' >' . $textarea->getValue() . '</textarea></div>';