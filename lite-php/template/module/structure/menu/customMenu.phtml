<?php
/**
 * @var \umicms\hmvc\view\CmsTreeView[] $tree
 * @var \umicms\hmvc\view\CmsTreeNode $node
 */
?>
<?php if (count($tree) > 0) : ?>
    <ul class="bottomMenu">
        <?php foreach ($tree as $node) : ?>
            <li>
                <a href="<?php $itemUrl = $node->item->getItemUrl(); echo !is_null($itemUrl) ? $itemUrl : '#'; ?>"
                    <?php echo $node->item->getItemType() == 'externalItem' ? 'target="blank"' : ''; ?> >
                    <?=$node->item->displayName?>
                </a>
                <?= $this->render('customMenu.phtml', ['tree' => $node->children]); ?>
            </li>
        <?php endforeach;?>
    </ul>
<?php endif; ?>